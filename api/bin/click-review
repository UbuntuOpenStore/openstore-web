#!/bin/env node

var exec = require('child_process').exec;
exec('click-review --help', {PYTHONPATH: '/app/.apt/usr/lib/python3/dist-packages/'}, function callback(error, stdout, stderr){
    console.log(error, stdout, stderr);
});
